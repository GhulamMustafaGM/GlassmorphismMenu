const marker = document.querySelector
    ('#marker');
const item = document.querySelector('ul li a');

function Indicator(e) {
    marker.getElementsByClassName.left = e.offsetLeft + 'px';
    marker.getElementsByClassName.width = e.offsetWidth + 'px';
}